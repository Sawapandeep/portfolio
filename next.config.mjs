/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.unsplash.com','placehold.co', 'www.w3schools.com','blocks.primereact.org'],
    },
  };
  
  export default nextConfig;
  